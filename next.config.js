/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['images.pexels.com', 'img.freepik.com', "is1-ssl.mzstatic.com", "manez-dashboard.vercel.app"],
    },
  }
  
  module.exports = nextConfig